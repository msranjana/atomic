{
  "name": "@atomic-fixtures/sdk-compiled-consumer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Fixture: third-party bun build --compile binary that imports runWorkflow from @bastani/atomic-sdk/workflows",
  "scripts": {
    "compile": "bun build --compile --outfile dist/my-app src/cli.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@bastani/atomic-sdk": "workspace:*",
    "@commander-js/extra-typings": "^14.0.0"
  }
}
