name: Publish
run-name: Legacy publish workflow is inert

# This path/name preserves the immutable identity of historical run 29529182569
# so its second attempt can emit one narrowly validated recovery completion.
# All privileged publication moved to publish-release.yml on protected main.
# Manual runs are inert and cannot satisfy the protected publisher's exact
# historical run-ID/attempt/tag/SHA allowlist.
on:
    workflow_dispatch:

permissions: {}

jobs:
    inert:
        name: Legacy workflow is inert
        runs-on: ubuntu-latest
        timeout-minutes: 5
        steps:
            - run: echo "This legacy workflow never publishes."
